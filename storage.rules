rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Allow read access to all files
    match /{allPaths=**} {
      allow read: if true;
    }
    
    // Categories folder - allow authenticated users to write
    match /categories/{fileName} {
      allow write: if request.auth != null;
      allow delete: if request.auth != null;
    }
    
    // Services folder - allow authenticated users to write  
    match /services/{fileName} {
      allow write: if request.auth != null;
      allow delete: if request.auth != null;
    }
    
    // Products folder - allow authenticated users to write
    match /products/{fileName} {
      allow write: if request.auth != null;
      allow delete: if request.auth != null;
    }
    
    // User uploads folder - allow users to manage their own files
    match /users/{userId}/{allPaths=**} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
